const add = (first, seconds) => {
    return first + seconds
}
const multiply = (first, seconds) => {
    return first * seconds
}

const divideTowNumverFirstAndSecond = (first, second) =>{
    return first / second;
}

export {add, multiply, divideTowNumverFirstAndSecond};